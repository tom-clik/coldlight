# Live Preview

To make editing the markdown easier, a live preview version will reload the markdown files when they change.

See e.g. `sample/preview/index.cfm`

This uses the same definitions as the static site but replaces all html links with `index.cfm?section={code}`.



